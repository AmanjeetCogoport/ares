package com.cogoport.ares.model.payment

enum class AccMode {
    AR, AP;
}

enum class AccType {
    SINV, PINV, SCN, SDN, PCN, PDN, REC, PAY;
}

enum class AllCurrencyTypes {
    GBP, EUR, USD, INR;
}
